import { StarHostDirective } from './star-host.directive';

describe('StarHostDirective', () => {
  it('should create an instance', () => {
    const directive = new StarHostDirective();
    expect(directive).toBeTruthy();
  });
});
